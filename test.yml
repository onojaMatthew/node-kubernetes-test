# apiVersion: v1
# kind: PersistentVolume
# metadata:
#   name: mongodb-pv
#   labels:
#     name: mongodb-pv
# spec:
#   capacity:
#     storage: 50Gi
#   storageClassName: standard
#   accessModes:
#     - readWriteOnce
#   gcePersistentDisk:
#     pdName: pg-data-disk
#     fsType: ext4  

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
  labels:
    type: local
spec:
  accessModes:
    - readWriteOnce
  resources:
    requests:
      storage: 50Gi
  volumeName: mongodb-pv
